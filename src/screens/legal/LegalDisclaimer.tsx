/** LegalDisclaimer — Trainingshinweis (gleicher Wortlaut wie DisclaimerGate, ohne Checkbox). */

import { DisclaimerText } from '../../components/DisclaimerText';

export function LegalDisclaimer() {
  return <DisclaimerText />;
}
